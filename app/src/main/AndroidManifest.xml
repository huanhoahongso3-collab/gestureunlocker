<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_lock_lock"
        android:label="Gesture Unlock"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestureUnlock">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Gesture Unlock Settings"
            android:theme="@style/Theme.GestureUnlock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RecordGestureActivity"
            android:exported="false"
            android:label="Record Gesture"
            android:theme="@style/Theme.GestureUnlock">
        </activity>

        <activity
            android:name=".GestureUnlockActivity"
            android:exported="true"
            android:showForAllUsers="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.GestureUnlock.Transparent">
        </activity>

        <receiver
            android:name=".UnlockWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/unlock_widget_info" />
        </receiver>

    </application>

</manifest>
